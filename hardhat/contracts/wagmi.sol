import "./LToken.sol";

/** @dev Those contracts are not to be deployed and are only a trick allowing Wagmi CLI to
 * generate properly named hooks for L-Tokens contracts.
 */

contract LEUROC is LToken {

}

contract LUSDC is LToken {}
