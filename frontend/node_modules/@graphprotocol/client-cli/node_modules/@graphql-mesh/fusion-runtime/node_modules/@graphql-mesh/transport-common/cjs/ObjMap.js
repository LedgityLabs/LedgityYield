"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ObjMapScalar = void 0;
const graphql_1 = require("graphql");
exports.ObjMapScalar = new graphql_1.GraphQLScalarType({
    name: 'ObjMap',
    serialize: value => {
        if (typeof value === 'string') {
            return value;
        }
        const stringifiedValue = JSON.stringify(value);
        // TODO: Doesn't work due to a bug in graphql-js
        // if (stringifiedValue.length <= 255) {
        //   return value;
        // }
        return stringifiedValue;
    },
    parseValue: value => {
        if (typeof value === 'object') {
            return value;
        }
        try {
            return JSON.parse(value.toString());
        }
        catch (e) {
            return value;
        }
    },
    parseLiteral: (ast, variables) => {
        if (ast.kind === 'StringValue') {
            try {
                return JSON.parse(ast.value);
            }
            catch (e) { }
        }
        return (0, graphql_1.valueFromASTUntyped)(ast, variables);
    },
});
