import type { ServerStartOptions, ServerStartResult } from './types.js';
export declare function startNodeHttpServer({ meshHTTPHandler, getBuiltMesh, sslCredentials, graphqlPath, hostname, port, }: ServerStartOptions): Promise<ServerStartResult>;
