import { Card } from "@/components/ui";
import { FC } from "react";

export const AdminRecover: FC = () => {
  return (
    <Card className="p-10">
      <p className="text-lg font-semibold">Not available yet</p>
    </Card>
  );
};
